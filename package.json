{
  "name": "mathtizzy",
  "version": "1.0.0",
  "private": true,
  "description": "Math facts practice app",
  "scripts": {
    "build": "node scripts/copy-www.mjs",
    "cap:sync": "npm run build && npx cap sync && node scripts/sync-android-module.mjs",
    "cap:android": "npm run cap:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor-community/text-to-speech": "^8.0.2",
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
